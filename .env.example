# Supabase Configuration
# Get these values from your Supabase project settings
# https://supabase.com/dashboard/project/_/settings/api

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Note: These are PUBLIC environment variables (prefix NEXT_PUBLIC_)
# They are safe to expose in the browser but should never contain secrets.
# The anon key is designed to be public and has Row-Level Security (RLS) policies
# to protect data access at the database level.
